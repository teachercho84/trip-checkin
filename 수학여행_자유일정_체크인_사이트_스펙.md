# 고2 수학여행 자유일정 모둠 체크인 관리 사이트 — 개발 스펙

## 1. 프로젝트 개요
고등학교 2학년 수학여행 중 자유일정(모둠별 자율 이동) 하루 동안, 각 모둠이 계획서(시간·장소)대로 체크인하며 이동하고, 교사가 실시간으로 전체 모둠의 진행 상황과 위치를 확인할 수 있는 웹 사이트.

- 대상 규모: 약 20개 모둠, 모둠당 4~6곳 방문, 행사 1일
- 학생: 로그인 없이 코드/링크로 접속 (단, 모둠장은 로그인)
- 교사: 로그인 후 전체 모둠 관리

---

## 2. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 프론트엔드 | React + Vite | 정적 빌드 결과물을 GitHub Pages에 배포 |
| 배포 | GitHub Pages | 무료 개인 계정은 **공개(public) 저장소** 필요. 배포된 사이트의 JS/HTML은 누구나 볼 수 있음(정상, anon key 노출 자체는 문제 아님) |
| 배포 자동화 | GitHub Actions | push 시 자동 빌드 → gh-pages 브랜치 배포 |
| 백엔드(DB/실시간/파일저장/인증) | Supabase | 무료 티어. 별도 서버 코드 없이 브라우저에서 Supabase SDK로 직접 통신 |
| 지도 | Google Maps JavaScript API | 마커 표시, 체크인 순서 연결선(직선) |
| 주소→좌표 변환 | Google Geocoding API | 엑셀 업로드 시 장소명 → 좌표 자동 변환 |
| 엑셀 파싱 | SheetJS(xlsx) 등 클라이언트 라이브러리 | 브라우저에서 직접 처리, 서버 불필요 |
| 사진 처리 | Canvas API로 클라이언트단 리사이즈/압축 | 업로드 전 처리, 서버 불필요 |

### 비용
이 규모(모둠 20개, 하루 행사)에서는 전부 무료 한도 내로 운영 가능.
- Supabase 무료: DB 500MB, 파일저장 1GB, 실시간 동시접속 200 — 단, **7일간 미접속 시 프로젝트 자동 일시정지** → 행사 며칠 전 미리 접속해서 깨워둘 것
- Google Maps/Geocoding: 월 1만 건 무료(Essentials 등급) — API 키 발급에 **결제수단(카드) 등록 필수**(초과분만 청구되므로, 예산 알림 설정 권장)
- GitHub Pages, GitHub Actions(공개 저장소): 무료

---

## 3. 사용자 역할 및 로그인 구조

### 3.1 역할 3종
1. **학생(모둠원)**: 로그인 없음. 모둠 코드/링크로 접속. 체크인 가능.
2. **모둠장**: 로그인 필요. 화면·기능은 학생과 동일 + 계정 소유자.
3. **교사**: 로그인 필요. 전체 모둠 조회, 연락처 관리, 계획서 업로드 등 관리 기능.

### 3.2 로그인 방식 (사용자에게 보이는 화면 기준)
- **교사**: 이름 + 비밀번호(본인 휴대폰번호)
- **모둠장**: 모둠명 + 비밀번호(모둠장 학번)
- 이메일 개념 없음. 사용자는 한글 이름/모둠명만 입력.

### 3.3 내부 구현 방식
- Supabase Auth는 이메일 기반이라 한글을 그대로 이메일에 쓸 수 없음.
- 교사/모둠마다 **내부 코드(t001, g001 등)를 자동 부여**하고, 한글 이름/모둠명 ↔ 내부 코드 매핑 테이블을 둠.
- 로그인 시: 입력된 한글 이름/모둠명 → 매핑 테이블에서 내부 코드 조회 → `t001@internal.local` 형태의 내부용 이메일로 변환 → Supabase Auth 로그인 처리.
- 로그인 성공 시 role(teacher / group_leader)과 소속 모둠 정보를 함께 반환받아 화면·권한을 분기.

### 3.4 계정 추가 방법
- **모둠장 계정**: 엑셀 `모둠정보` 시트에 학번 컬럼 포함 → 업로드 시 자동으로 로그인 계정 생성/갱신. 재업로드 시 신규 모둠은 생성, 기존 모둠은 정보만 갱신.
- **교사 계정**: 설정 탭 내 별도 폼(이름 + 휴대폰번호 입력)으로 등록. 소수 인원이라 엑셀 불필요.

### 3.5 권한(RLS) 원칙
- 학생/모둠장: 자기 모둠 데이터만 읽기/쓰기(체크인) 가능.
- 교사: 전체 모둠 데이터 읽기 가능. 연락처 등 개인정보는 **교사 role만 열람 가능**하도록 명확히 제한.
- 학생 화면에서는 모둠원/모둠장 연락처 등 개인정보 노출 금지.

---

## 4. 데이터 구조

### 4.1 엑셀 업로드 양식 (교사가 한 파일에 전체 모둠 정보 포함, 시트 2개)

**시트1: 모둠정보**

| 모둠명 | 모둠장 | 학번 | 모둠장 연락처 | 모둠원 |
|---|---|---|---|---|
| 1모둠 | 김민준 | 20401 | 010-1234-5678 | 김민준, 이서연, 박지훈 |

**시트2: 타임테이블**

| 모둠명 | 시간 | 장소 | 할일 |
|---|---|---|---|
| 1모둠 | 10:00 | 국제시장 | 씨앗호떡 먹고 인증사진 찍기 |
| 1모둠 | 14:00 | 남포동 거리 | 자유 쇼핑 |

- "할일"은 선택 항목(비워둬도 됨) — 비어있으면 학생 화면에는 장소명만 표시.
- 두 시트는 "모둠명"으로 매칭. 철자가 정확히 일치해야 함.
- 업로드 후 시스템이 "인식된 모둠명 목록"을 보여줘서 오타로 인한 모둠 쪼개짐을 즉시 확인할 수 있게 함.
- 장소명은 업로드 시 Geocoding API로 좌표 자동 변환. 변환 실패 항목은 빨간색으로 표시하고 설정 탭에서 수동으로 재검색/수정 가능하게 함(장소 검색 자동완성 UI 권장).

### 4.2 비상연락망 데이터
- 담임교사 3명(1반/2반/3반), 버스기사 2명(전체 공통) — 이름 + 전화번호.
- 설정 탭에서 교사가 별도로 등록/관리 (엑셀과 무관, 자주 안 바뀌는 고정 정보).
- 전화번호는 `tel:` 링크로 연결해 탭 시 바로 통화 연결.

### 4.3 체크인 데이터
- 체크인 시 저장 항목: 모둠명, 방문지(계획서 항목과 매칭), 체크인 시각, 위치 좌표(1회 캡처), 사진.
- 위치는 `getCurrentPosition()`으로 체크인 순간 1회만 캡처(상시 추적 아님).
- 사진은 업로드 전 클라이언트에서 리사이즈(가로 최대 1280px) + JPEG 압축(품질 약 70%)하여 장당 약 150~300KB로 최적화.

---

## 5. 화면 구성

### 5.1 학생 화면 (모바일, 하단 탭 3개, 순서: 일정 → 경로 → 연락처)

**① 일정 탭**
- 접속 시 자기 모둠의 계획서(시간·장소·할일) 전체를 시간순으로 표시 (별도 "계획서 보기" 화면 불필요, 이 화면이 곧 계획서).
- 각 항목에 시간·장소 아래 "할일"을 작은 텍스트로 함께 표시(등록된 경우).
- 완료된 항목: 초록색, 체크인 시각 표시.
- 지금 처리할 항목: 강조 카드로 표시, 할일 문구 + "사진 찍기" + "체크인하기" 버튼.
- 아직 안 된 향후 항목: 흐리게 "예정" 표시.

**② 경로 탭**
- 체크인한 순서대로 번호(①②③...)가 매겨진 지도.
- 체크인 좌표를 시간순으로 직선 연결(1차 구현. 추후 필요 시 Directions API로 실제 도로 경로선으로 고도화 가능).
- 하단에 체크인 순서·장소·시각 리스트.

**③ 연락처 탭**
- 담임교사 3명 + 버스기사 2명 목록, 탭하면 바로 전화 연결(`tel:`).

### 5.2 교사 화면 (모바일, 하단 탭 3개: 현황판 / 계획서 / 설정)

**① 현황판 탭**
- 상단 요약: 전체 모둠 수, 체크인 완료 수, 지연 모둠 수.
- 지도: 전체 모둠의 체크인 마커 표시 (모둠 20개 마커 밀집 방지를 위해 마커 클러스터링 적용 권장).
- 모둠 리스트: 모둠별 체크인 진행률 + 마지막 체크인 시각/장소. 카드 클릭 시 모둠 상세로 이동.

**모둠 상세 (현황판에서 진입)**
- 상단: 모둠장/모둠원 이름, 모둠장 연락처(교사만 열람 가능).
- 계획(시간·장소·할일) vs 실제 체크인 시각 타임라인 비교. 완료/지연/예정 상태를 색으로 구분.
- 지도: 해당 모둠의 체크인 위치만 표시(예정 위치 회색, 완료 위치는 색상 마커, 체크인 순서 연결선).

**② 계획서 탭**
- 모둠 선택 시 상단에 모둠장/모둠원/연락처, 하단에 해당 모둠 타임테이블 표시.
- (참고: 20개 모둠 전체를 한 화면에 나열하는 뷰도 필요 시 추가 가능 — 이 경우 모둠 수가 많아지므로 필터/검색 UI 권장)

**③ 설정 탭**
- 모둠별 계획서 엑셀 업로드(위 4.1 양식), 업로드 후 모둠별 등록 현황(등록완료/미등록/좌표변환실패) 표시.
- 비상연락망(담임교사, 버스기사) 등록/수정.
- 교사 계정 추가(이름 + 휴대폰번호 입력 폼).

---

## 6. 기타 유의사항
- 학생 코드/모둠장 계정 정보(비밀번호=학번)는 유출 시 타 모둠이 접근 가능한 수준의 보안이므로, 하루짜리 행사 용도로는 충분하나 더 엄격한 보안이 필요하면 별도 확인 절차 추가를 고려할 것.
- 위치 정보 및 사진은 개인정보에 해당할 수 있어, 학생/학부모 대상 사전 안내 또는 학교 정책 확인 권장.
- 행사 종료 후 데이터 보관 기간 정책(예: 일정 기간 후 자동 삭제) 필요 여부 검토.
